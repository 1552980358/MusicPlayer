<resources
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CloudPieceMusicPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="Q">?attr/isLightTheme</item>

        <item name="materialCardViewStyle">?attr/materialCardViewFilledStyle</item>
    </style>

    <style name="Theme.CloudPieceMusicPlayer" parent="Base.Theme.CloudPieceMusicPlayer" />
    
    <style name="Theme.CloudPieceMusicPlayer.LauncherActivity" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?attr/colorPrimary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.CloudPieceMusicPlayer</item>
    </style>
    
</resources>