<resources
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CloudPieceMusicPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="Q">?attr/isLightTheme</item>

        <item name="circleShapeableImageAppearance">@style/Widget.MaterialComponents.ShapeableImageView.RoundCorner.Circle</item>
        <item name="roundShapeableImageAppearance">@style/Widget.MaterialComponents.ShapeableImageView.RoundCorner.Full</item>
    </style>

</resources>